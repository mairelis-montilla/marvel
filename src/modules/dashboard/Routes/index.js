
import MarvelList from '../Pages/MarvelList';

const routes = [
  {
    path: '/',
    component: MarvelList,
    /* children: [
      {
        path: '',
        name: 'marvel-list',
        component: MarvelList,
      }
    ] */
  }
]

export default routes